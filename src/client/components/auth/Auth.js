import React from 'react';
import MainScreen from "./MainScreen";

function Auth() {
    return (
        <main>
            <div className="MainFlex">
                <MainScreen/>
            </div>
        </main>

    );
}

export default Auth;